# database

应用首次启动后会在此目录生成 `finderos.db`，并自动完成从 v0.1 到 v0.2 的增量迁移。

v0.2 数据覆盖：

- 用户、角色、功能、菜单和角色授权关系
- 瞭源及采集规则配置
- 采集结果与数据仓库记录
- 模型配置、默认模型状态和 token 用量统计

数据库及 `-shm`、`-wal` 临时文件只属于本机运行状态，不进入源码 ZIP。模型 API Key、Cookie Secret 和浏览器 Cookie 也不得写入本目录或提交包。
