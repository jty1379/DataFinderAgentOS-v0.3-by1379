开发 DataFinderAgentOS v0.5 时，先完整阅读 role.md、constraint.md、requirement.md、template.md 和 test_case.md。
实现顺序遵循老师的 2.x → 3.x → 4.x：先修复超级管理员和只读管理角色，再实现瞭源、瞭望采集与数据仓库，最后实现 OpenAI 兼容模型引擎和 SSE。
保持 MVC + Service 分层、安全基线、本地化 UI 和双端模板边界；新增功能必须同步维护数据库迁移、权限功能、菜单、测试和文档。
默认 admin 禁止被修改或删除，只有该账号能写入用户、角色、功能、菜单四个权限配置模块；其他管理角色在这些模块只读。瞭源、瞭望采集、数据仓库、模型引擎和模型聊天按 role_feature 授权，获授权的管理角色可以使用和维护。
外部采集不得复制浏览器 Cookie，自动测试必须 mock；目标站失败要如实反馈，禁止用演示数据冒充真实结果。
模型 API Key 不得回显、记录或打包；Controller 不直连模型 SDK，SSE 断开后必须停止上游生成。
深度采集由系统采集专员真实执行，任务、进度、步骤、日志与结果必须持久化；失败不得伪装成功。
数字员工支持模型/Crawl4AI 与 HTTP API 两类，统一通过 Service 调度；用户侧通过 / 或 @ 调度已启用员工，系统采集专员只在后台运行。
所有依赖安装、启动与测试必须使用项目 venv。深采必须真实调用 Crawl4AI；@天气必须读取 wttr.in JSON；会话和消息按用户持久化隔离。
任务 5.1：数字员工新增/编辑支持多 Markdown 上传，按 data/dgUser/<员工ID>/ 隔离，资料只补充事实背景且不打包。
任务 6.1—6.4：用户模型请求使用 OpenAI stream=true + SSE，显示耗时、token 和配置名称；统计/趋势/深采/关系/图谱/报告优先读取真实仓库数据并按意图绘图。
绝不接收、执行或回显用户 SQL；SQL 与 Prompt 注入在 Service 层先拒绝，分析查询只能来自固定只读 Repository。
